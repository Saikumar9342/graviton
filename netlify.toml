[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

# This plugin is essential for Next.js to work on Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Standard build settings
# Redirects are now handled via frontend/public/_redirects for better compatibility with Next.js plugin
